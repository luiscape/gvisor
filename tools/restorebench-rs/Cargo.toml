[package]
name = "restorebench-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "=1.0.219", features = ["derive"] }
serde_json = { version = "=1.0.142" }
libc = { version = "=0.2.172" }

[profile.release]
opt-level = 3
lto = true
